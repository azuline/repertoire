import * as React from 'react';

import { SectionHeader } from 'src/components';

export const NotFound: React.FC = () => {
  return <SectionHeader className="my-8 px-8 text-center">You are lost :V</SectionHeader>;
};

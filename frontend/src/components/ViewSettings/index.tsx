import * as React from 'react';
import { RVOCType } from 'src/hooks';

export const ViewSettings: React.FC<{ viewOptions: RVOCType }> = ({ viewOptions }) => {
  return (
    <div className="flex">
      <div></div>
    </div>
  );
};
